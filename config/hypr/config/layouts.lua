hl.config({
    dwindle = {
        preserve_split = true,
        special_scale_factor = 1.0,
    },
})

hl.config({
    master = {
        new_status = "master",
        special_scale_factor = 1.0,
    },
})

hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
    },
})
